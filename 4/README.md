# Test::Differences and Test::Exception demo

To run:

  perl exceptions_differences.t

Note the file open failure caught by dies_ok and the successful open checked
by lives_ok(). The differences test is obvious.
